name: lovely_shop_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.0
  fluttertoast: ^8.2.2
  provider: ^6.1.1  # ✅ provider 패키지 추가
  path_provider: ^2.1.1  # 파일 저장소 접근용
  cupertino_icons: ^1.0.2
  google_mobile_ads: ^6.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/
